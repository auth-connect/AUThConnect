apiVersion: v1
kind: Service
metadata:
  name: {{include "postgres.fullname" . }}
  namespace: postgres
spec:
  ports:
    - port: {{.Values.port}}
      targetPort: {{.Values.port}}
      protocol: TCP
  selector:
    app: postgres
