apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auth-connect.fullname" . }}-test-connection"
  labels:
    {{- include "auth-connect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auth-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
